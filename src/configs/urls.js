const baseURL = 'http://owu.linkpc.net/api/v2';

// const baseURL = 'https://jsonplaceholder.typicode.com'

const urls = {
    users: '/users',
    posts: '/posts',
}

export {
    urls,
    baseURL,
}